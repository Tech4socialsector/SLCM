# Interview Module — Test Scenario Matrix Table

| ID | Module | Feature | Title / Objective | Pre-conditions | Test Steps | Test Data | Expected Result | Actual Result | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC_INT_001** | Admission | Interview Configuration | **Ratio Format Validation**: Validates ratio formats like `3` (1:3 ratio) or `1:3` pass regex validation. | Active Admission Module; Interview Configuration form accessible. | 1. Open **Interview Configuration** form.<br>2. Set Enter Domestic Ratio to "3".<br>3. Set Enter International Ratio to "1:3".<br>4. Click Save. | Domestic Ratio = "3", International Ratio = "1:3" | Validation passes cleanly without throwing format error. | System validated and saved ratio inputs successfully. | **Pass** |
| **TC_INT_002** | Admission | Interview Configuration | **Invalid Ratio Format Rejection**: System blocks invalid ratio format strings like `-1` or `abc`. | Validation pattern regex active. | 1. Open **Interview Configuration** form.<br>2. Set Enter Domestic Ratio to "invalid".<br>3. Click Save.<br>4. Observe validation error message. | Domestic Ratio = "invalid" | System blocks saving and asks for positive integer or X:Y format. | Frappe validation engine raised validation exception blocking save. | **Pass** |
| **TC_INT_003** | Admission | Interview Configuration | **Configuration Code Auto-Generation**: Automatically generates an 8-character uppercase hash code before saving. | Auto-code generation hook active. | 1. Create a new Interview Configuration document.<br>2. Fill mandatory fields (Campus, Academic Year, Cycle).<br>3. Save document.<br>4. Inspect `configuration_code`. | New Interview Configuration record | System generates and assigns an 8-character uppercase code. | Document saved with auto-generated 8-character code. | **Pass** |
| **TC_INT_004** | Admission | Candidate Extraction | **Source 1: Direct National Test Exemption**: Extracts candidates exempt from entrance exam but requiring interview. | National Test Exemption record created (`exempts_entrance_test=1`, `exempts_interview=0`). | 1. Create candidate with National Test exemption.<br>2. Run `get_eligible_applicants()`.<br>3. Check returned candidate list. | Applicant with Entrance Test Exemption | Candidate included in pool with `source_type` = "National Test (Direct)". | Candidate extracted into eligible pool with source "National Test (Direct)". | **Pass** |
| **TC_INT_005** | Admission | Candidate Extraction | **Source 2: Entrance Test Passers**: Extracts candidates who passed entrance examination. | Entrance Test Seat Allocation record with `result_status = 'Pass'`. | 1. Generate Entrance Test seat allocation with passing marks.<br>2. Run `get_eligible_applicants()`.<br>3. Check returned candidate list. | Entrance Test Result = "Pass", Score = 80 | Candidate included in pool with `source_type` = "Entrance Test". | Candidate extracted into eligible pool with source "Entrance Test". | **Pass** |
| **TC_INT_006** | Admission | Candidate Extraction | **Source 3: Direct Academic Eligibility**: Extracts candidates for non-entrance test programs. | Programme configured with `entrance_test = 0`, `intereview = 1`. | 1. Create candidate for non-entrance test program.<br>2. Run `get_eligible_applicants()`.<br>3. Check returned candidate list. | Program without Entrance Test | Candidate included in pool with `source_type` = "Academic Eligibility". | Candidate extracted into eligible pool with source "Academic Eligibility". | **Pass** |
| **TC_INT_007** | Admission | Candidate Filtering | **Exclusion of Already Scheduled Applicants**: Candidates already in an Interview List are skipped. | Candidate already added to an Interview List child row. | 1. Add candidate to an existing Interview List.<br>2. Run candidate pool extraction.<br>3. Verify candidate is excluded. | Applicant ID present in `tabInterview Applicant` | System excludes candidate to prevent duplicate interview scheduling. | Candidate present in existing list was excluded from new pool. | **Pass** |
| **TC_INT_008** | Admission | Candidate Filtering | **Exclusion of Rejected Applicants**: Candidates with status `'Rejected'` are excluded. | Applicant status set to "Rejected". | 1. Set applicant status to "Rejected".<br>2. Run candidate pool extraction.<br>3. Verify candidate is excluded. | Applicant status = "Rejected" | Rejected candidate is skipped from candidate pool. | Rejected candidate excluded cleanly from candidate pool. | **Pass** |
| **TC_INT_009** | Admission | Candidate Filtering | **Domestic vs International Stage Flag Filter**: Applies correct stage requirements based on nationality. | Programme stage flags configured. | 1. Test domestic candidate with `intereview = 1`.<br>2. Test foreign candidate with `international_interview = 1`.<br>3. Check pool extraction. | Foreign National = "Yes" vs "No" | System checks `intereview=1` for domestic, `international_interview=1` for foreign. | Stage flags filtered candidates according to nationality flag. | **Pass** |
| **TC_INT_010** | Admission | Quota Calculation | **Domestic Applicant Quota Lookup**: Fetches total domestic seats from Programme Reservation Policy. | Active `Programme Reservation Policy` for target campus and cycle. | 1. Set `applicant_type` to "Domestic Applicants".<br>2. Call `get_total_seats()`.<br>3. Verify returned seat count. | Policy Total Seats = 60 | System returns `total_seats` configured for domestic applicants. | Method fetched exact total seats (60) from policy. | **Pass** |
| **TC_INT_011** | Admission | Quota Calculation | **International Applicant Quota Lookup**: Fetches total international seats from policy. | Active `Programme Reservation Policy` with foreign seat quota. | 1. Set `applicant_type` to "International Applicants".<br>2. Call `get_total_seats()`.<br>3. Verify returned seat count. | Policy International Seats = 10 | System returns `international_seats` configured for foreign applicants. | Method fetched exact international seats (10) from policy. | **Pass** |
| **TC_INT_012** | Admission | Quota Calculation | **Combined Both Applicants Quota Lookup**: Sums domestic and international seats for `applicant_type = "Both"`. | Active policy with domestic & international seats. | 1. Set `applicant_type` to "Both".<br>2. Call `get_total_seats()`.<br>3. Verify returned total sum. | Domestic = 60, International = 10 | System returns total sum of domestic + international seats (70). | Method returned combined sum of 70 total seats. | **Pass** |
| **TC_INT_013** | Admission | Selection Engine | **Selection Multiplier Quota Calculation**: Applies multiplier formula `ceil(seats * multiplier)`. | 10 seats, domestic ratio = "3" (1:3 ratio). | 1. Set seats = 10 and ratio = "3".<br>2. Execute selection simulation.<br>3. Check number of candidates selected. | Seats = 10, Multiplier = 3.0 | System selects top 30 candidates (`ceil(10 * 3.0)`). | System calculated quota as 30 and selected top 30 ranked candidates. | **Pass** |
| **TC_INT_014** | Admission | Selection Engine | **Fetch Exempted Applicants Bypass Flag**: Setting `fetch_exempted_applicant = 1` bypasses ratio cap. | `fetch_exempted_applicant = 1` enabled on configuration. | 1. Set `fetch_exempted_applicant = 1`.<br>2. Run `generate_interview_list()`.<br>3. Check number of selected candidates. | 50 candidates, 10 seats, ratio = "2" | All 50 eligible candidates are selected regardless of ratio cap (20). | Multiplier cap bypassed; all 50 eligible candidates selected. | **Pass** |
| **TC_INT_015** | Admission | Selection Engine | **Cumulative Rank Ascending Sort**: Candidates ordered primarily by `cumulative_rank` ascending. | Multiple candidates with varying cumulative ranks. | 1. Populate pool with candidates of ranks 5, 1, 3, 2.<br>2. Run selection engine.<br>3. Inspect order of selected list. | Ranks: [5, 1, 3, 2] | Higher-ranked candidates (lower rank number: 1, 2, 3, 5) appear first. | Candidate pool sorted in strict ascending rank order. | **Pass** |
| **TC_INT_016** | Admission | Selection Engine | **Cutoff Boundary Equal Rank Tie-Breaking**: Includes all candidates sharing exact rank at cutoff line. | Candidates tied at cutoff line (e.g. 3 candidates tied at rank 10). | 1. Set quota cap = 10.<br>2. Provide candidate pool where candidates 10, 11, 12 share rank 10.<br>3. Run selection engine. | Quota = 10, Tied Rank = 10 | All candidates tied at the cutoff rank are included in selection (12 total). | Selection engine included all tied candidates at cutoff line without dropping any. | **Pass** |
| **TC_INT_017** | Admission | List Generation | **Zero Candidate Exception & Failure Status**: Sets status to "Failed" when candidate pool is empty. | Candidate pool contains 0 eligible applicants. | 1. Create configuration for program with 0 applicants.<br>2. Call `generate_interview_list()`.<br>3. Catch expected exception.<br>4. Inspect configuration status. | 0 eligible candidates | System sets status to "Failed" and throws "No Candidates Found" error message. | Configuration status set to "Failed"; structured error message thrown. | **Pass** |
| **TC_INT_018** | Admission | Interview List | **Interview List Auto Naming Sequence**: Generates Interview List with pattern `IVL-{year}-001`. | Academic Year set to "2026-2027". | 1. Generate Interview List for Academic Year 2026.<br>2. Check `name` field on created document. | Academic Year = "2026-2027" | Document name follows pattern "IVL-2026-2027-001". | Interview List generated with name `IVL-2026-2027-001`. | **Pass** |
| **TC_INT_019** | Admission | Interview List | **Naming Sequence Gap Filling**: Reuses lowest available sequence number for deleted lists. | Existing list `IVL-2026-002` deleted from database. | 1. Delete list `IVL-2026-002`.<br>2. Create a new Interview List for 2026.<br>3. Inspect generated name. | Deleted Name = "IVL-2026-002" | System reuses lowest available sequence number ("IVL-2026-002"). | Sequence gap-filling re-assigned name `IVL-2026-002` to new list. | **Pass** |
| **TC_INT_020** | Admission | Slot Allocation | **Inactive Staff Member Assignment Block**: Blocks allocating slots to an inactive interviewer. | Interview Staff Member created with `is_active = 0`. | 1. Select staff member with `is_active = 0`.<br>2. Call `allocate_interview_slots()`.<br>3. Observe validation error. | Staff `is_active = 0` | System blocks allocation and throws error stating staff member is not active. | Frappe exception raised blocking slot allocation for inactive interviewer. | **Pass** |
| **TC_INT_021** | Admission | Slot Allocation | **Panel Double-Booking Overlap Prevention**: Prevents booking same interviewer at same date/time twice. | Staff member already booked for scheduled interview at 2026-09-01 10:00. | 1. Book Staff A on 2026-09-01 at 10:00.<br>2. Try booking Staff A again on 2026-09-01 at 10:00.<br>3. Observe validation error. | Date = "2026-09-01", Time = "10:00:00" | System blocks allocation with double-booking overlap warning. | Duplicate booking blocked with panel overlap error message. | **Pass** |
| **TC_INT_022** | Admission | Slot Allocation | **Program Stage Interview Enabled Check**: Validates program has `intereview = 1` in stages. | Programme with `intereview = 0`. | 1. Try allocating interview slot for program with `intereview = 0`.<br>2. Observe validation error. | Programme `intereview = 0` | System blocks allocation with stage disabled message. | Allocation rejected because program stage did not have interview enabled. | **Pass** |
| **TC_INT_023** | Admission | Slot Allocation | **Interview Seat Allocation Record Generation**: Successfully allocating slots creates child records. | Active staff member and selected candidate list. | 1. Select active staff member and candidate list.<br>2. Set date and time.<br>3. Call `allocate_interview_slots()`.<br>4. Check created allocations. | Date = "2026-09-05", Time = "10:00:00" | System creates `Interview Seat Allocation` records with `interview_status = 'Scheduled'`. | `Interview Seat Allocation` record created with status "Scheduled". | **Pass** |
| **TC_INT_024** | Admission | Slot Allocation | **Candidate Category Resolution Sync**: Child category rows automatically populated from Applicant. | Applicant holding categories `["General", "OBC", "Karnataka"]`. | 1. Allocate slot for applicant with categories.<br>2. Open created `Interview Seat Allocation`.<br>3. Check child category table. | Categories = ["OBC", "Karnataka"] | System automatically fetches and populates candidate categories. | Category child rows populated with ["OBC", "Karnataka"]. | **Pass** |
| **TC_INT_025** | Admission | Status Synchronization | **Applicant Status Propagation**: Updates Applicant document status to `'Interview Scheduled'`. | Applicant status set to "Submitted". | 1. Allocate interview slot for candidate.<br>2. Open Applicant document.<br>3. Check `status` field. | Target Applicant ID | System updates Applicant document status in database to "Interview Scheduled". | Applicant document status updated in MariaDB database to "Interview Scheduled". | **Pass** |
